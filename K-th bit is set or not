// Amazon Google Microsoft

class Solution {
    public bool CheckKthBit(int n, int k) {
        // Bit wise and between n and 1 left shifted to k positions
        if((n & 1 << k) != 0)
            return true;
        else
            return false;
    }
}
